# EPUB Studio Mobile v7.9.43 — 목차 인식 종합 점검판

## v7.9.43 변경 (이번 판)

- **Calibre로 변환된 EPUB에서 화 제목이 '알 수 없음'으로 뜨던 문제 수정 (예: '스펠바운드').** Calibre 변환본은 (1) 목차(NCX)가 사실상 비어 있고, (2) 모든 파일의 `<title>`을 '알 수 없음'으로 채우며, (3) 화 번호를 `<div class="number-item">15.</div>`처럼 담고, (4) 본문 XHTML에 XML 오류가 있어 파싱이 실패하는 경우가 많습니다. 이 때문에 제목이 '알 수 없음'이나 파일명(`index_split_000.html`)으로 떨어졌습니다.
  - `<title>`이 '알 수 없음/Unknown/제목 없음' 같은 placeholder면 무시합니다.
  - 헤딩이 없으면 `number-item`의 화 번호를 제목으로 씁니다.
  - **XML 파싱이 실패해도 정규식으로 제목(헤딩·번호·본문 첫 줄)을 뽑아** 파일명으로 떨어지지 않게 했습니다.



## v7.9.42 변경

- **EPUB 합본 생성이 `sgc-nav.css → href 누락`으로 실패하던 문제 수정 (예: '새벽을 여는 범' 합본, 4권 원본).** 원인은 원본 EPUB(Sigil 계열) 일부가 스타일시트를 `../Styles/sgc-nav.css`가 아니라 **`sgc-nav.css`처럼 디렉터리가 빠진 잘못된 경로**로 링크하고 있었기 때문입니다. 관대한 뷰어는 넘어가지만, 합본 시 각 권을 하위 폴더로 옮기면 이 깨진 링크가 실제로 없는 파일을 가리켜 내부 검증이 막았습니다.
  - 스타일시트 링크를 재작성할 때, 경로가 어긋나면 **파일명(basename)으로도 실제 CSS를 찾아** 올바른 경로로 연결합니다.
  - 링크 속성 순서(`href`가 `rel`보다 앞에 오는 경우 등)와 무관하게 처리합니다.
  - 어떤 실제 CSS에도 연결되지 않는 '깨진 스타일시트 링크'는 안전하게 제거해 생성이 막히지 않게 합니다.



## v7.9.41 변경

- **`1권: 1화~30화` 같은 화 번호 범위가 'Chapter NN'·'#N장' 형식 파일에서 안 되던 문제 수정.** 범위 기능이 화 번호를 읽을 때 `1화`·`1.`·`제1화`·`01`만 인식하고 **`Chapter 01`, `Chapter 05_return`, `#1장`은 번호로 못 읽어서**, 영문 챕터/해시 형식 파일에 범위를 쓰면 한 화도 못 찾았습니다. 이제 이 형식들도 화 번호로 인식합니다. (범위 파싱 자체는 공백 유무와 무관하게 정상이었고, 매칭 단계의 번호 인식이 문제였습니다.)



## v7.9.40 변경

- **권 경계로 갈리는 파트 화(예: '내 편' `Chapter 05_return (1)`/`(2)`)가 배정 안 되던 문제 수정.** 본문에는 3권에 `Chapter 05_return`(파트 번호 없이) 한 번만 있고, `(2)`에 해당하는 내용은 4권 앞부분에 **제목 없이 이어지는** 구조였습니다.
  - `(1)`,`(2)` 같은 괄호 파트를 파트 표기로 인식하도록 했습니다.
  - 본문에 파트 라벨이 없으면 기본 제목으로 매칭해 `(1)`을 본문의 해당 화에 연결합니다(본문에 `(1)`/`(2)`가 실제로 있는 파일은 종전대로 정확히 구분).
  - `(2)` 이상 이어지는 파트는 해당 권 **시작부(제목 없이 이어지는 내용)** 를 찾아 배정합니다.
- 이전 판에서 편집이 중간에 끊겨 목차 붙여넣기 매칭이 실행 중 오류로 동작하지 않던 문제도 함께 해결했습니다.



## v7.9.39 변경

- **목차에서 화 번호를 다시 매긴 파일에서 1권이 통째로 미배정되던 문제 수정 (예: '7분의 천국').** 목차는 `1~8`로 번호를 매겼는데 본문은 원래 번호(`1, 8, 9, 10 …`)를 유지한 경우, 제목이 같아도(`2. Untouchable` ↔ 본문 `8. Untouchable`) 번호가 달라 매칭이 막혔습니다(원래 날짜·'Momentum 1장/2장' 구분용 규칙이 과하게 적용됨). 이제 **`N. 제목` 형식에서 번호 뒤 '제목'이 같으면 앞 번호가 달라도 같은 화**로 인식합니다. 날짜(`1992년 6월 14일` vs `15일`), `Momentum 1장`/`2장`, `3.(1)`/`3.(2)`처럼 번호 자체가 정체성인 경우는 종전대로 구분합니다.



## v7.9.38 변경

- **한 파일에 여러 권이 `7분의 천국 1권@TNT`처럼 인라인으로 든 합본 TXT의 '권 구분'을 못 잡던 문제 수정 (예: '7분의 천국' 1-4권 합본).** 권 마커 인식이 `1권`(단독)·`[2권]`만 알아서, `책제목 N권@워터마크` 형태를 권 구분으로 인식하지 못했습니다. 그 결과 4개 권이 한 덩어리로 합쳐져, 권마다 화 번호가 재시작(1권 9~14 ↔ 2권 9~14)하는 목차의 매칭이 어긋났습니다. 이제 `제목 N권@…` / `제목 N권(외전)` 형태도 권 구분으로 인식해, 권별로 정확히 나눕니다. (본문 속 '3권의 책을 샀다' 같은 문장은 권으로 오인하지 않습니다.)
- 참고: 이 파일처럼 화 제목이 `1. 제목` 형식이고 **내부 목차(목차/차례)가 없는** 경우, 본문 속에도 `1. …` 같은 목록이 많아 '자동 감지'만으로는 화와 목록을 구분하기 어렵습니다. 이럴 때는 **목차를 붙여넣어** 주시면 정확히 매칭됩니다.



## v7.9.37 변경

- **`Chapter 05_return (1)`처럼 'Chapter 번호' 뒤에 접미사·제목·파트가 붙은 화를 자동 감지 못하던 문제 수정.** 본문 패턴 감지(`chapterLine`)가 `Chapter 05`처럼 **번호에서 줄이 끝나야만** 인식해서, `Chapter 05_return (1)` / `(2)`는 번호 뒤에 `_return (1)`이 붙어 탈락했습니다. 이제 `Chapter N` 뒤에 밑줄·공백으로 이어지는 접미사/제목/파트를 허용합니다(줄 길이 60자 이내). 지나치게 긴 산문 줄은 화로 잡지 않습니다.



## v7.9.36 변경

- **v7.9.35 방식의 부작용 제거 + 더 안전한 방식으로 재수정.**
  - v7.9.35에서 '순수 번호 목차는 번호만인 본문에만 매칭'하게 했더니, `목차: 1.` ↔ `본문: 1. 만남`처럼 **목차는 번호만, 본문은 번호+제목**인 정상 파일이 매칭되지 않는 부작용이 있었습니다. 이 변경을 **되돌렸습니다**(`1.`↔`1. 만남`, `1화`↔`1화 만남` 다시 정상).
  - 대신 본문 속 하위 목록(`1. 술 끊기 …`)은 **같은 권·같은 문맥에서 번호가 직전 화보다 갑자기 낮아지고(예: 12→1) 바로 앞 마커와 매우 가까운(≈120줄 이내) 경우에만** 화가 아닌 것으로 걸러냅니다. 진짜 화는 수백~수천 줄 간격이라 영향받지 않습니다.
  - 결과: `12.(2)` 본문 정상 유지 + 번호/제목 혼합 목차의 정상 매칭 모두 보존.



## v7.9.35 변경

- **화 본문 안의 '하위 목록'이 가짜 화로 잡혀 본문이 비던 문제 수정 (예: '먼저 줍는 놈이 주인' 5권 `12.(2)`).** 12.(2) 화 본문에 들어 있던 목록 `1. 술 끊기 / 2. 담배 끊기 / 3. 바람피우지 않기`가 목차의 `1.`,`2.`,`3.`(1권 화)과 매칭되어 가짜 화 시작점이 되는 바람에, 12.(2)의 본문이 그 앞(빈 줄)에서 잘려 '본문 없음'이 됐습니다. 이제 **순수 번호 목차(`1.`,`2.`)는 본문도 '번호만'인 줄에만 매칭**하고, `1. 술 끊기`처럼 번호 뒤에 제목이 붙은 본문 속 목록은 화로 잡지 않습니다. (v7.9.34에서 12.(2)가 '배정'은 됐지만 본문이 비던 후속 문제를 해결)



## v7.9.34 변경

- **각 권의 '첫 화'가 미배정으로 빠지던 문제 수정 (예: '먼저 줍는 놈이 주인' 2권 `3.(2)`·`4.`, 5권 `12.(2)`).** 내부 목차 범위 감지(`detectInternalTocRange`)가 본문 시작점을 못 찾으면 목차를 최대 120줄까지 삼켜, 목차 바로 뒤에 오는 첫 화가 '목차의 일부'로 오인돼 건너뛰어졌습니다. 특히 첫 화가 대사(`'…!'`)나 짧은 하위 목록으로 시작하면 산문 판정이 실패했습니다. 이제 `extractEmbeddedToc`와 동일하게 **'목차의 첫 항목이 본문에서 다시 나오면 그 지점을 본문 시작'**으로 인식해, 첫 화를 삼키지 않습니다. 후보로는 잡히는데 4-1에서 미배정되던 증상이 해결됩니다.



## v7.9.33 변경

- **내부 목차의 화 항목 인식 두 가지 버그 수정 (예: '먼저 줍는 놈이 주인').**
  - 평범한 `1.`, `2.`, `4.`처럼 **숫자+마침표로 끝나는 목차 항목**이 인식되지 않아, `3. (1)` 같은 파트 항목만 잡히고 평번호 화를 통째로 놓치던 문제를 고쳤습니다.
  - `외전 1.`, `외전 2.`처럼 **한글 키워드 뒤에 공백이 오는 항목**이 인식되지 않던 문제(정규식 단어경계 `\b`가 한글 뒤에서 성립하지 않던 것)를 고쳤습니다.
  - 이제 `1. / 2. / 3. (1) / 3. (2) / … / 외전 1. / 외전 2.`처럼 단순 번호·파트 번호·외전이 섞인 목차도 각 화로 정확히 잡습니다.



## v7.9.32 변경

- **'2-1.', '2-2.', '4-2.' 같은 '부(部) 번호' 화를 범위로 오해해 못 잡던 문제 수정.** 예전에는 목차의 `2-2.`를 '2~2 범위'로, `1-3.`을 '1,2,3'으로 잘못 확장해서 실제 본문의 `2-2.` 화와 매칭되지 않았습니다(예: '납치범들'). 이제 **하이픈+마침표로 끝나는 `N-M.`은 범위가 아니라 한 화 제목('N화 M부')**으로 그대로 둡니다. 진짜 범위 입력(`1~13`, `1-13`, `14화~24화`)은 종전대로 정상 확장합니다.



## v7.9.31 변경

- **잘못 중첩된 EPUB 목차(NCX/NAV)로 목차를 못 잡던 문제 수정.** 일부 EPUB(예: txt2pub 변환본 '길들인 짐승')은 목차에서 외전 화들이 마지막 본편 화(예: '7 재회, 그리고 순응') 아래에 잘못 중첩되어 있습니다. 예전에는 이 마지막 화가 '그룹(권) 제목'으로 취급되어 화 목록에서 빠지고 매칭이 어긋났습니다. 이제 어떤 목차 항목이 '실제 본문 화'(구분 표지가 아닌 본문)를 가리키면서 자식 항목까지 달고 있으면, 그 항목과 자식들을 모두 형제 화로 평탄화해 같은 그룹에 넣습니다. 본문이 없는 진짜 권 구분 표지(v7.9.30) 아래의 화들은 종전대로 권 그룹으로 유지합니다.



## v7.9.30 변경

- **권 구분 표지 페이지가 '미배치'로 잡히던 문제 수정.** 한 EPUB 안에 `페어 앤 버티컬 러브 1권`·`2권`·`외전`처럼 본문 없이 권 제목만 있는 짧은 구분 페이지가 여러 개 들어 있는 경우, 이 페이지들은 목차(NCX/NAV)에서 권(그룹) 제목이 되어 화로는 배정되지 않습니다. 예전에는 이들이 '미배치(미사용 본문)'로 집계되어 개수가 뜨고 엄격 검증 시 생성이 차단됐습니다. 이제 '권 구분 표지 페이지'(본문이 거의 없고 제목이 N권·N부·외전 등으로 끝나는 짧은 페이지)를 부가 페이지로 인식해, 미배치에서 빼고 '제외 부가페이지'로 분류합니다. 실제 본문이 있는 화(제목이 '3권'이어도 본문이 길면)는 그대로 유지합니다.



## v7.9.29 변경

- **'#' 접두 화 제목 자동 인식** — `#1장. 1%`, `#외전 1.`처럼 **# + 번호/장/외전 + 제목** 형식(예: '영웅의 관')도 자동으로 화를 잡습니다. 기존에는 `#N`(숫자만)일 때만 인식해, 뒤에 `장. 제목`이 붙으면 놓쳤습니다. `#N장`·`#N화`·`#외전 N`·`#프롤로그`·`#에필로그` 등을 지원합니다.



## v7.9.28 변경

- **표지·설명 이미지 삭제/취소 기능 추가.** 이미지를 잘못 넣었을 때 지울 수 있습니다.
  - 대표 표지: '표지 선택 취소' 버튼 + 선택한 이미지 미리보기.
  - 작품 설명 이미지(여러 장): 선택한 이미지가 썸네일 목록으로 표시되고, 각 이미지의 ✕ 버튼으로 **개별 삭제**, '설명 이미지 전체 삭제' 버튼으로 **한 번에 삭제**할 수 있습니다. 삭제한 이미지는 최종 EPUB에도 포함되지 않습니다.



## v7.9.27 변경

- **맨 숫자(01, 02 …)만으로 구분된 화 자동 인식** — 내부 목차가 없고 마침표·'화' 없이 `01`, `02`처럼 **숫자만 있는 줄**로 화가 나뉜 파일(예: '사소한 사이')도 자동으로 잡습니다. 오탐 방지를 위해 '앞뒤가 빈 줄인 고립된 숫자'가 '순차 증가'로 2개 이상, 서로 충분히 떨어져 있을 때만 화로 인식하며, 일반 패턴으로 이미 화가 잡힌 파일에는 개입하지 않습니다.



## v7.9.26 변경

- **자동 배열 결과가 '목차 붙여넣기' 칸에 다시 채워집니다.** EPUB 합본(다권 EPUB)을 자동 분석하면 4-1 목차는 만들어지는데 수동 편집용 목차 칸(textarea)은 비어 있어, 자동 배열을 그 칸에서 손볼 수 없었습니다. 이제 자동 분석·'원본 목차로 자동 구성'·후보 체크 변경 시 모두 목차 칸이 현재 배열대로 채워져, 그대로 보고 수정할 수 있습니다.



## v7.9.25 변경

- **한자 숫자 화 제목 자동 인식** — 내부 목차가 없고 화 제목이 `一. 새옹지마`, `二. 일기일회`, `十七. 후회유기`처럼 **한자 숫자(一二三…十七)**로 된 파일(예: '불야성')도 자동으로 화를 잡습니다. `서막序幕_…`처럼 서막·종막·서장·종장류(뒤에 한자·언더바 장식이 붙는 형태)도 화로 인식합니다.
- **긴 문단 가독성 분리 개선**
  - `sentenceSplit`에 전각 문장부호(`。！？‥⋯`) 인식 추가.
  - '특수 클래스 문단 제외' 옵션을 정교화 — 예전에는 class/id가 있으면 **모든** 문단을 건너뛰어 Calibre 변환 EPUB(`<p class="calibre2">`)처럼 모든 문단에 클래스가 붙은 경우 가독성 분리가 아예 안 먹었습니다. 이제 메세지·댓글·인용·이미지·표 등 **특수 연출 문단만 보호**하고, calibre2·p1·bodytext 같은 **일반 본문 문단은 분리**합니다.
  - 참고: 가독성 분리는 '긴 문단 가독성 개선 실행'을 켠 경우에만 동작하며, '최소 문단 길이'(기본 300자)보다 긴 문단만 대상입니다.



## v7.9.24 변경

- **표지·목차 체크 해제가 4-1 목차에 반영되도록 수정 (중요 버그)** — 원본 목차(nav/ncx)에는 표지·내부목차 페이지도 항목으로 남아 있어, 그 페이지를 후보에서 빼면 프로그램이 '그 자리에 다음 후보를 순서대로' 억지로 채워 넣어 **표지 → 01처럼 화가 한 칸씩 밀리는** 문제가 있었습니다. 이제 제외된 페이지에 해당하는 목차 항목은 건너뛰어, 표지·목차를 빼면 밀림 없이 1화부터 제자리에 배정됩니다. (EPUB 합본·단일 EPUB 모두 적용)
- **문단 사이 간격 기본값 0.5em → 0.8em**으로 변경. (설정에서 계속 조절 가능)
- **가독성 '안전/강한 모드' 설명 명확화** — 화면에 두 모드의 실제 차이를 표기.
  - 공통: '최소 문단 길이'(기본 300자)보다 긴 문단만 문장 단위로 나눔. 짧은 문단·대사는 손대지 않음.
  - 안전 모드: 한 조각에 **최소 2문장**은 남김(덜 잘게 나눔).
  - 강한 모드: '문단당 목표 문장 수'를 **그대로** 적용(1이면 1문장씩, 더 잘게 나눔).



"목차 잡는 게 전반적으로 불안정하다"는 문제를 케이스별로 재현·수정했습니다. 아래 형식들을 **자동으로** 처리합니다.

---

## 지원하는 목차 형식

### 권(볼륨) 구분
- `[1권]` 대괄호형
- `<2권>` 꺾쇠형 (그리고 `【3권】`·`《외전 권》` 등 전각 괄호)
- `제4권`, `1권`
- `5권 (외전)`, `6권 (외전2)` → **권으로 인식**
- (별도 외전권은 `외전 권` 또는 `[권] 외전`)

### 화(챕터)
- 번호만: `1`, `2`, `01`
- 한글/임의 제목: `어느날`, `그날 밤`
- `1화`, `제3화`, `1장`, `제2장` 등
- **날짜형 제목**: `1992년 6월 14일 일요일`처럼 날짜가 곧 화 제목인 일기/연재 형식
  - 서로 다른 날짜(요일까지 달라도)를 유사도로 잘못 합치지 않습니다
  - **에필로그의 중복 날짜**(예: 1권 첫 화와 같은 `1992년 6월 14일`)도 각각 제 위치에 정확히 배정

### EPUB 내부 화 자동 감지
- heading(h1~h6)이 없고 `<p><b>01.</b></p>`처럼 **굵은 숫자 문단**으로만 화가 구분된 EPUB(Calibre 변환본 등)도 자동으로 화를 나눕니다.
- 한 권이 여러 파일(index_split_000/001/002 …)로 나뉘어 있어도, 그 안의 굵은 숫자 화를 모두 찾아 쪼갭니다.
- **오탐 방지**: 본문 속에 우연히 들어간 숫자(예: 삐삐 화면 `0 1 2 4.`, `6.25` 같은 표기)는 화로 잡지 않습니다. `<b>`/`<strong>`로 강조됐거나 heading 태그인 순수 번호가 2개 이상일 때만 화로 인식합니다.
- 목차 없이 자동으로 잡히지만, 원하면 `1권` 아래 `1 ~ 13`처럼 범위를 붙여넣어 명시할 수도 있습니다.

### 상위 구분(문맥) — 그 아래 화 번호가 재시작돼도 OK
- `chapter 1`, `Chapter 1` (대소문자 무관)
- `Chapter One`, `Chapter Two` … (영어 숫자 단어)
- `[chapter 1]` (괄호로 감싼 형태)
- `part 1`, `파트 1`
- `제1장 / 1장`, `제1부 / 1부`
- **각 구분 아래에서 `1·2·3`처럼 번호가 다시 시작해도** 문맥별로 서로 다른 본문에 정확히 배정
- **구분 제목 자체에 본문이 있어도, 없어도, 섞여 있어도** 모두 처리
  - `장 / 부`는 그 아래 번호가 재시작되면 상위 문맥으로, 아니면 그냥 회차로 **자동 판단**

> 오탐 방지: `1장면 전환`·`1부분만`·`장난감`·`부산 여행`처럼 우연히 장/부가 들어간 일반 제목은 문맥으로 잡지 않습니다.

---

## 무엇이 문제였고 어떻게 고쳤나 (요약)

1. **꺾쇠·전각 괄호 권(`<2권>` 등)을 못 잡던 문제** — 권 헤더/마커 판정을 대괄호·꺾쇠·전각 괄호 모두 인식하도록 확장하고 괄호를 벗겨 정규화.
2. **chapter 번호 재시작에서 뒤 화가 비던 문제(가장 심각)** — 본문 시작을 "제목 반복 + **바로 다음 실제 줄이 산문**"일 때로 판정(`nextNonBlankIsProse`). 구분선(`* * *`)·워터마크(`@…`)는 건너뜀. 내부 목차 범위 탐지와 자동 추출 양쪽에 적용.
3. **순수 번호(`1`, `01`) 헤딩이 매칭 안 되던 문제** — 숫자만으로 된 한 줄을 헤딩 번호로 인정.
4. **본문 없는 chapter가 생성을 막던 문제** — chapter/part 같은 구조 제목은 미배정이어도 차단 대상에서 제외.
5. **chapter 표기 확장** — `Chapter One`(영어 숫자단어)·`[chapter 1]`(괄호형)·`제1장/1장`·`제1부/1부`를 문맥으로 인식. 영어 숫자단어는 번호로 환산해 `Chapter One`/`Chapter Two` 라벨이 뒤섞이지 않게 처리.
6. **날짜형 목차가 서로 섞이던 문제** — `1992년 6월 14일`처럼 날짜가 제목이면, 기존 매칭이 맨 앞 숫자(연도 1992)만 보고 유사도로 이웃 날짜까지 끌어와 권 배정이 뒤엉켰습니다. → **양쪽 다 숫자를 포함하고 숫자열(선행 0 무시)이 다르면 유사도와 무관하게 다른 화**로 판정. 요일이 달라 어간 비교를 우회하는 경우까지 차단. 중복 날짜(에필로그 등)도 권 구간·순서로 정확히 배정.
7. **EPUB '굵은 숫자' 화 자동 감지** — heading 없이 `<p><b>01.</b></p>`로만 화가 구분된 EPUB도 자동 분할. 권이 여러 파일로 나뉘어 있어도 그 안의 번호 화를 모두 찾음(순수 번호 화 2개 이상 + 굵게/heading 조건). 본문 속 숫자(삐삐 화면 등)는 오탐 제외.
8. **(이전 판 유지) "차례" 내부 목차 인식** — 헤더가 `목차`·`차례`·`목 차`·`contents`여도 인식, 워터마크·산문 오인 방지, 1화짜리 권 정상 분리.

> 목차 파서/매칭 엔진의 핵심 알고리즘은 유지했습니다. **인식 범위(헤더·항목·권 괄호·chapter 표기)와 헤딩-번호 인식, 본문 시작 판정**만 정교화했습니다.

---

## 검증 (모두 실제 코드 실행으로 확인)

- **`_할로윈_핏탕물_1-9권.zip`**: 9권 전부 정상 분리, **23화(01~22 + 외전), 빈 본문 0** — 회귀 통과
- `[1권]` + 번호/한글제목, `<1권>`/`<2권>` 꺾쇠 권, `5권 (외전)`/`6권 (외전2)` — 정확
- **chapter 재시작**(각 chapter 아래 1·2·3화): 문맥별로 서로 다른 올바른 본문에 배정, 미배정 0
- **새 4형식 종합 테스트**
  - `Chapter One / Chapter Two` 재시작 — 라벨·본문 정확
  - `제1장 / 제2장` 재시작 — 정확, 미배정 0
  - `1장 / 2장 / 3장`(재시작 없음 = 회차) — 각 장이 회차로 정확 매칭
  - `제1부 / 제2부` 재시작 — 정확, 미배정 0
- **본문 없는 구조 제목**(chapter 1 등): 미배정으로 남아도 생성 차단되지 않음
- 오탐 방지(`1장면`·`1부분`·`장난감`·`부산`): 문맥으로 안 잡힘
- **날짜형 목차(일기 형식)**: 권별 날짜 배정 정확, 서로 다른 날짜 안 섞임, 에필로그 중복 날짜도 제자리 배정, 미배정 0
- **실제 EPUB 7권(`_쇼타_나이트크럽_연정.zip`)**: heading 없이 굵은 숫자로만 구분된 형식 → **7권 전체 77화(1~77 연속) 자동 분할 정확**, 4권 본문 속 숫자(삐삐 화면) 오탐 0
- 전체 스크립트 문법 검사 통과

---

## 사용 팁

- **합본(한 TXT에 여러 권)**: 파일 안에 `<2권>`·`[3권]`·`제4권`·`5권 (외전)` 같은 줄이 있으면 자동으로 권을 나눕니다.
- **붙여넣는 목차**: 권 헤더는 `[1권]`·`<2권>`·`5권 (외전)`처럼, 하위 화는 그 아래 `1`/`어느날`/`1화`처럼. 상위 구분은 `chapter 1`·`제1장`·`제1부` 등으로 두고 번호를 재시작해도 됩니다.
- **chapter 재시작이 있는 파일**은 목차를 붙여넣는 방식이 가장 정확합니다(문맥 구분이 확실해짐).
- `장`/`부`가 실제로는 회차인지 상위 구분인지는 그 아래 번호 재시작 여부로 자동 판단하지만, 애매하면 목차를 붙여넣어 명확히 해주세요.
- 특이 형식으로 목차가 안 잡히는 파일이 있으면 그 파일의 **목차 헤더 표기와 제목 몇 줄**을 알려주시면 바로 반영하겠습니다.

---

## 자주 하는 실수 (중요)

- **목차와 본문의 형식은 같아야 합니다.** EPUB 본문의 화 제목이 숫자(`01. 02.`)인데 목차 칸에 날짜(`1992년 6월 14일`)를 붙여넣으면 서로 매칭되지 않아 "미배정·본문 없음"이 대량 발생합니다.
- **홈페이지(연재 사이트) 목차**를 그대로 붙여넣을 때는, 그 목차의 화 개수·순서가 EPUB 실제 화와 정확히 같은지 먼저 확인하세요. 사이트에서 회차를 합치거나 빠뜨린 경우 개수가 어긋나 뒤로 갈수록 다 밀립니다.
- 숫자로만 구분된 EPUB은 **목차 칸을 비우고 자동**으로 두는 것이 가장 확실합니다. 굳이 넣으려면 `1권` 아래 `1 ~ 13`처럼 숫자/범위로 넣으세요(날짜 X).
