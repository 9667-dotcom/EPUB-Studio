# EPUB Studio Mobile v7.9.6 README

## 1. 파일 구성

- `index.html` : EPUB/TXT 분석, 목차 재구성, 합본, 가독성 수정 및 EPUB 생성 프로그램
- `READM.md` : 현재 버전 사용법과 주의사항

브라우저에서 HTML 파일을 직접 열어 사용합니다. 파일 처리는 브라우저 안에서 진행됩니다.

---

## 2. 지원 입력

- 단일 EPUB
- 단일 TXT
- 여러 EPUB 직접 선택
- TXT 여러 개가 들어 있는 ZIP
- EPUB 여러 개가 들어 있는 ZIP
- EPUB + TXT가 함께 들어 있는 ZIP

TXT 인코딩은 자동 감지, UTF-8, EUC-KR/CP949를 선택할 수 있습니다.

---

## 3. 작업 모드

### 1) EPUB 합본·편집

표지, 작품 설명, 권 구분, 목차 재구성, 가독성 옵션까지 사용하는 기본 모드입니다.

권이 있는 EPUB은 기본적으로 다음 구조를 목표로 합니다.

```text
대표 표지
작품 설명
1권 전용 페이지
 ├─ 1화
 ├─ 2화
 └─ ...
2권 전용 페이지
 ├─ ...
외전 / 특별편
 ├─ ...
```

NAV와 NCX는 권 → 화의 2단계 계층 구조로 생성됩니다.

### 2) 가독성·정렬만 적용

단일 EPUB의 표지, 목차, spine, OPF 구조를 재구성하지 않고 가독성 관련 옵션만 적용합니다.

---

## 4. 기본 작업 순서

1. 원본 파일 선택
2. `원본 분석`
3. 필요하면 대표 표지 선택
4. 작품 소개 / BL 가이드 / 기타 작품 설명 입력
5. 목차 입력 또는 `원본 목차로 자동 구성`
6. 감지된 화 후보와 목차 연결 상태 확인
7. 필요하면 권/화 제목, 배정, 순서 수정
8. 생성 전 검사 확인
9. EPUB 생성
10. 생성 결과의 XML, 내부 링크, ZIP, CSS 검사 결과 확인

---

## 5. 목차 입력

### 권 → 화 구조

```text
1권
1화
2화
3화

2권
4화
5화

[권] 외전
외전 1화
외전 2화
```

`[권] 외전`처럼 입력하면 외전을 상위 권/구분으로 만듭니다.

그냥 `외전`, `외전 1` 등은 화 제목으로 유지됩니다.

### 화 번호 범위 지정

```text
1권: 1화 ~ 28화
2권: 29화 ~ 56화
3권: 57화 ~ 84화
```

본문 후보를 먼저 분석한 뒤 사용할 수 있습니다.

### 평평한 목차

단일 연재본처럼 권 페이지가 필요 없는 경우 사용할 수 있습니다.

**여러 EPUB 합본에서는 평평한 목차 사용을 전제로 하지 않습니다.** 여러 EPUB 합본은 권 → 화 구조로 사용하는 것을 기준으로 합니다.

---

## 6. 판권·광고·미리보기 제거 — v7.9.2 수정 사항

`판권·광고·구매 안내·미리보기·기존 내부 목차 제거`를 켜면 단일 EPUB 분석에서도 해당 옵션이 실제 후보 판정에 반영됩니다.

대상 예:

- 판권
- 출판사 광고
- 구매 안내
- 다음 권 안내
- 미리보기 / 체험판
- 기존 본문용 목차

이 옵션을 **원본 분석 후 변경한 경우에는 다시 `원본 분석`을 실행하는 것이 안전합니다.** 분석 시점에 본문 후보가 결정되기 때문입니다.

실제 본문인지 애매한 항목은 감지된 화 후보 화면에서 직접 확인하는 것을 권장합니다.

---

## 7. 이미지 위주 XHTML — v7.9.2 수정 사항

이미지 또는 SVG 위주로 구성된 XHTML을 단순히 이미지가 있다는 이유만으로 자동 삭제하지 않습니다.

이런 페이지는 후보 확인 화면에서 대략 다음 의미로 표시됩니다.

```text
이미지 페이지 / 이미지 위주 페이지
삽화·문자·특수 연출 가능 — 자동 보존
```

즉 다음과 같은 페이지가 실수로 사라지는 것을 줄이기 위한 처리입니다.

- 삽화
- 문자/채팅 연출
- 편지나 메일 이미지
- 배경 이미지 기반 특수 연출
- SVG 연출

파일명이나 제목, EPUB 메타데이터 등에 `cover`, `표지`, `titlepage` 등 명확한 표지 근거가 있는 경우에는 표지로 판정할 수 있습니다.

---

## 8. 가독성 옵션

현재 검토본은 가독성 기능을 적극적으로 사용하는 설정을 유지합니다.

### 좌우 정렬을 왼쪽 정렬로 변경

기본 ON.

`text-align: justify` 계열을 `left`로 바꿔 Moon+ Reader 등에서 단어 사이가 과도하게 벌어지는 현상을 줄입니다.

### 문단 사이 간격

기본 ON.

본문 `<p>`에 문단 간격과 첫 줄 들여쓰기를 추가합니다.

기본값:

- 문단 간격: `0.5em`
- 첫 줄 들여쓰기: `0.5em`

메시지, 댓글, 채팅, 말풍선 등으로 추정되는 특수 클래스는 가능한 한 건드리지 않도록 되어 있습니다.

### 긴 문단 가독성 개선

별도 선택 기능입니다.

- 안전 모드
- 강한 모드

기본적으로 class/id, 내부 태그, 이미지/SVG/script가 있는 문단을 제외하도록 되어 있습니다.

**특수 서식 보존이 중요한 EPUB에서는 이 보호 옵션을 유지하는 것을 권장합니다.**

---

## 9. CSS 보존과 가독성 수정

원본 EPUB의 CSS 파일은 가능한 한 그대로 보존하며, 합본 시 각 원본 EPUB의 리소스는 독립 경로에 복사합니다.

단, `좌우 정렬을 왼쪽 정렬로 변경`을 사용하면 `text-align: justify` 선언은 의도적으로 변경됩니다.

따라서 생성 결과의 CSS 대조 표시는 다음 두 개념을 구분해서 봐야 합니다.

- 원본 CSS 파일이 누락되지 않았는지
- 사용자가 요청한 justify 변경 결과가 예상대로 적용됐는지

---

## 10. 생성 차단 옵션

`미배정 목차·미사용 원본·중복·불일치가 있으면 생성 차단`을 켜 두면 다음 문제가 남아 있을 때 생성이 중단될 수 있습니다.

- 목차에 있지만 본문이 연결되지 않은 화
- 후보로 잡혔지만 최종 목차에 사용되지 않은 본문
- 같은 원본 본문을 둘 이상의 화에 중복 배정
- 목차 제목과 원본 제목의 대응이 지나치게 낮음

실제 합본 작업에서는 이 옵션을 켜 두는 편이 안전합니다.

---

## 11. 생성 후 자동 검사

일반 EPUB 생성 과정에서는 다음을 검사합니다.

- `mimetype`이 ZIP 첫 항목인지
- `mimetype` 무압축 여부
- `application/epub+zip` 내용
- `META-INF/container.xml`
- OPF XML 파싱
- NAV XML 파싱
- NCX XML 파싱
- XHTML / SVG XML 파싱
- manifest 파일 존재 여부
- spine idref 유효성
- NAV 링크
- NCX 링크
- 내부 href/src 등의 실제 파일 존재 여부
- fragment/id 링크
- NAV/NCX 항목 수
- NCX playOrder
- CSS 대조
- ZIP CRC32

검사 실패 시 정상 파일로 간주하지 않습니다.

---

## 12. 현재 검토가 더 필요한 부분

### 여러 EPUB 합본에서 NAV에 직접 없는 XHTML

일부 EPUB은 한 화가 여러 XHTML로 나뉘어 있는데 NAV/NCX에는 그중 첫 파일만 표시될 수 있습니다.

현재 자동 판정에서는 NAV에 직접 없는 일부 XHTML을 부가 페이지로 판단할 가능성이 남아 있습니다.

따라서 여러 EPUB을 합칠 때는 **`감지된 화 후보 확인`에서 자동 제외된 항목을 한 번 확인하는 것이 안전합니다.** 특히 파일명이 일반 본문처럼 보이거나 이미지/특수 연출이 포함된 페이지라면 임의로 제외하지 않는 것이 좋습니다.

이 부분은 다음 검토에서 자동 판정을 더 보수적으로 바꿀 예정인 항목입니다.

---

## 13. 권장 사용 설정

일반적인 소설 합본 기준 권장값:

```text
표지 작업                         ON
작품 설명 작업                    ON (필요 시)
목차·권 페이지 재구성             ON
좌우 정렬 → 왼쪽 정렬             ON
문단 사이 간격                    ON
긴 문단 가독성 개선               필요할 때만 ON
판권·광고·미리보기 제거           ON
동일 본문 중복 제거               ON
엄격 매핑                         ON
CSS 대조                          ON
```

자동 판정 결과가 이상하면 생성부터 하지 말고 먼저 후보/목차 편집 화면에서 수정합니다.

---

## 14. v7.9.6 변경 요약

- 단일 EPUB에서도 판권·광고 제거 옵션이 분석에 반영되도록 수정
- 이미지/SVG 위주 XHTML을 단순 이미지 페이지라는 이유만으로 자동 삭제하지 않도록 변경
- 이미지 위주 페이지를 사용자가 확인할 수 있도록 상태/후보 정보 강화
- 여러 EPUB 합본의 권 → 화 구조는 기존 방식 유지
- `1.`, `4.`, `４．`, `4`처럼 숫자 회차 표기 차이를 같은 회차로 인식
- TXT 자동 감지에서 `1.`, `2.`, `3.` 형식 지원
- 자동 감지된 원본 목차가 `1화 → 10화 → 2화` 순서면 숫자 회차 연속 구간만 자연수 순서로 보정
- 사용자가 직접 붙여넣은 목차 순서는 자동 정렬하지 않음
- 권별 범위 배정과 다권 EPUB 권별 위치 매칭에도 자연수 순서 보정
- 가독성 관련 기본 사용 방향 유지



### v7.9.4 추가 수정
- 원본 EPUB 개수와 사용자가 지정한 최종 권 수가 다르면 권별 위치 매칭을 사용하지 않고 제목 기반 전역 검색으로 자동 전환합니다.
- 한 XHTML 안에 `에필로그.`와 `외전1.`처럼 여러 목차 제목이 함께 있으면 각각 가상 목차 후보로 인식합니다.
- 가상 목차는 원본 XHTML을 물리적으로 쪼개지 않고 제목 위치에 내부 앵커(id)를 추가해 NAV/NCX에서 각각 정확한 위치로 이동합니다.
- 사용자가 `외전.`으로 입력하고 원문이 `외전1.`인 경우 일반 외전 제목으로 매칭할 수 있습니다.
- 원본 CSS, 문단, 이미지 및 본문 리소스는 그대로 보존하고 목차 연결만 세분화합니다.
- 실제 테스트 ZIP `[윈리드]낯선 하루.zip` 기준으로 `0~8장 + 에필로그 + 외전 + Happy New Year` 구조를 4권으로 재편성하는 케이스를 대상으로 수정했습니다.


### v7.9.5 권 이동 수정
- 목차 편집에서 한 권의 회차를 다른 권으로 옮길 때 화면 상태와 최종 생성 프로젝트 상태를 동시에 갱신합니다.
- `다른 권으로 이동` 드롭다운에서 대상 권을 선택하면 즉시 이동합니다. 기존 `이동` 버튼도 보조 기능으로 유지합니다.
- 회차를 다른 권의 영역/행으로 드래그해서 옮길 수 있습니다.
- 같은 권 안의 위/아래 이동도 동일한 프로젝트 커밋 경로를 사용해 최종 EPUB에 확실히 반영됩니다.
- 회차 제목 입력칸이 포커스를 잃을 때 목차 전체를 즉시 다시 그리지 않도록 수정했습니다. 이 때문에 대상 권 드롭다운 선택이 중간에 초기화되는 현상을 방지합니다.
- 최종 생성 시 편집 화면의 `state.groups`를 우선 기준으로 사용해 과거 프로젝트 스냅샷이 권 이동을 덮어쓰지 않도록 했습니다.


### v7.9.6 한 XHTML 내부 목차 자동 감지
- 여러 EPUB 합본의 **원본 분석 단계 자체에서** 본문 XHTML이 하나뿐인 EPUB의 내부 문단을 추가 검사합니다.
- `에필로그.`, `외전1.`, `후기` 같은 구조 제목을 붙여넣은 목차 없이도 자동 후보로 만듭니다.
- 첫 실제 본문 앞의 영문 제목(예: `Happy New Year.`)도 제목 다음에 본문이 이어지는 형태라면 자동 감지합니다.
- `공유금지 only toki`, `@...`, 파일명/권 머리말은 제목 후보에서 제외합니다.
- 파일명에 `에필로그 포함` 같은 설명이 있더라도 실제 본문의 `에필로그.`를 파일명 일부로 오인해 버리지 않도록 비교 조건을 강화했습니다.
- 원본 XHTML은 물리적으로 분할하지 않고 각 제목 위치에 내부 앵커만 추가합니다.
- 후보 확인 화면에 `내부 목차 감지: ...`가 표시됩니다.
- 동일 XHTML에 여러 자동 후보가 있을 때 NCX의 `Start` 하나가 마지막 후보로 덮어쓰던 문제를 수정해 원문 순서대로 연결합니다.
- `[윈리드]낯선 하루.zip` 회귀 테스트: 원본 분석 직후 본문 후보 12개 / 내부 XHTML 자동 감지 3개, `에필로그. / 외전1. / Happy New Year.` 모두 확인.
- 같은 샘플로 4권 재구성 후 EPUB 실제 생성 검증: NAV/NCX에 세 제목 모두 존재, 내부 앵커 3개 생성, ZIP CRC 정상.
