# EPUB Studio Mobile v7.9.9

## 간헐적 `couldn't read` / file permission 오류 개선

원인:
- 모바일 Chrome/Android 파일 선택기는 실제 파일 바이트가 아니라 일시적인 파일 접근 권한을 제공할 수 있습니다.
- 파일 선택 후 시간이 지나거나, 메모리 압박/앱 전환/클라우드 파일 제공자 상태 변화가 있으면 기존 `File` 참조가 읽히지 않을 수 있습니다.
- 이전 버전은 대표 표지·권별 표지 등 일부 파일을 생성 시점에 다시 읽었습니다.

수정:
- 원본 EPUB/TXT/ZIP 선택 즉시 ArrayBuffer 캐시
- 대표 표지 선택 즉시 캐시
- 작품 설명 이미지 선택 즉시 캐시
- 권별 새 표지 선택 즉시 캐시
- 프로젝트 JSON 불러오기도 동일한 안전 읽기 함수 사용
- 이후 분석/생성 단계는 동일 파일의 캐시된 바이트를 우선 사용
- 직접 `file.arrayBuffer()`를 호출하던 대표/권별 표지 생성 경로 제거
- arrayBuffer 실패 시 FileReader 재시도 후 명확한 오류 표시

화면에서 `준비 완료`가 표시된 파일은 이후 원본 파일 권한이 끊겨도 캐시를 사용합니다.

주의:
- 페이지 새로고침/초기화 시 브라우저 메모리 캐시는 사라지므로 파일을 다시 선택해야 합니다.
- 매우 큰 파일 여러 개를 동시에 선택하면 메모리 사용량이 증가할 수 있습니다.
