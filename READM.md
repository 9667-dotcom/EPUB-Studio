# EPUB Studio Mobile v7.9.11

## Chapter/Part별 반복 번호 매칭 수정

다음과 같은 목차를 지원합니다.

```text
[1권]
Chapter. 1: O Fortuna
01.
02.

[3권]
Chapter 2. Odorem Veritatis
01.
02.

[4권]
03.
Chapter 3. 업
01.
02.
```

### 수정 내용
- 같은 TXT 안에서 `01.`이 여러 번 나와도 첫 번째 하나만 남기던 전역 중복 제거 로직 수정
- 후보 중복 키를 `Chapter/Part 문맥 + 제목 + 분할 suffix`로 변경
- 붙여넣은 목차를 파싱할 때 현재 `Chapter N...` / `Part N...` 문맥을 각 회차에 저장
- 숫자 권 경계(1권 → 2권 등)에서는 Chapter 문맥을 유지
- `[외전]`, `[특별편]` 등 별도 구분으로 넘어가면 이전 Chapter 문맥을 초기화
- 매칭 시 동일한 제목이 여러 개면 현재 Chapter/Part 문맥이 같은 원문을 우선 선택
- 중복 목차 경고도 `권 + Chapter/Part 문맥 + 제목` 기준으로 변경

### 실제 테스트
업로드된 `[아르곤18]는개지구49 5完_외전1.txt`와 사용자가 제공한 목차로 검사:
- 목차 항목 29개
- 실제 본문 매칭 29개
- 미배정 0개
- `Chapter 2 / 01.` → 원문 두 번째 `01.` 정상 연결
- `Chapter 3 / 01.` → 원문 세 번째 `01.` 정상 연결
- JavaScript 문법 검사 통과
